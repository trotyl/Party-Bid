header.header
  label.nav-title 活动列表
  button(ng-click="activity_create()").button.button-rounded.button-highlight.header-right 创建活动

div.container.content
  div(ng-repeat="activity in activities | orderBy:'-createdAt'").list-button
    button(ng-click="show_activity_detail(activity.name)",class="button glow button-rounded btn-block button-large {{check_in_progress(activity.register ,activity.bid)}}") {{activity.name}}