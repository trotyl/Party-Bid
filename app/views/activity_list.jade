header.header
  label.nav-title 活动列表
  button.button.button-rounded.button-highlight.header-right-trotyl(ng-click="activity_create()",ng-disabled="cannot_create") 创建活动

.container.content
  .list-button(ng-repeat="activity in activity_list | orderBy:'-createdAt'")
    button(ng-click="show_activity_detail(activity)",class="button glow button-rounded btn-block button-large {{check_in_progress(activity)}}") {{activity.name}}
